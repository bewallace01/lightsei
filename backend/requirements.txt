fastapi==0.115.0
uvicorn[standard]==0.30.6
pydantic==2.9.2
sqlalchemy==2.0.36
alembic==1.13.3
psycopg[binary]==3.2.3
bcrypt==4.2.0
email-validator==2.2.0
cryptography==43.0.3
python-multipart==0.0.20
jsonschema==4.23.0
# Phase 9.2: dispatcher HTTP-out for Slack/Discord/Teams/Mattermost/webhook.
httpx==0.27.2
# Phase 12B.1: backend calls Claude for the agent generator.
anthropic>=0.40.0
# Phase 17.4: Stripe-hosted Checkout + Customer Portal + webhook signing.
stripe==15.1.0
# Phase 22.2: cron expression parsing + next-fire calculation for the
# scheduled-triggers surface. Same primitive used by Airflow, Prefect,
# Dagster; handles DST correctly so 9am-every-weekday means 9am local.
croniter==3.0.3
# Phase 28.2: web push delivery (VAPID-signed payloads). pywebpush
# bundles py-vapid + http_ece + handles the encryption + JWT signing
# Chrome/Firefox/Safari expect. Lazy-imported inside push.send so the
# test suite stays network-free without it installed locally.
pywebpush==2.0.3
